import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.rifatttc.bdp2p',
  appName: 'BDP2P',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
